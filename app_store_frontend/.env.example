# Frontend environment variables (CRA)
# Set ONE of these to point the SPA at the backend API.
REACT_APP_API_BASE=https://localhost:8000
# Alternatively:
REACT_APP_BACKEND_URL=https://localhost:8000

# Optional (not required by this SPA yet, but available in the container env list):
REACT_APP_FRONTEND_URL=http://localhost:3000
REACT_APP_WS_URL=ws://localhost:8000
